<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="profile">
	
	<select id="countPeed" parameterType="sist.co.model.MemberDTO" resultType="int">
		SELECT /* +INDEX_FFS(INSTA_PEED,SYS_C007790) */ NVL(COUNT(*),0) 
			FROM INSTA_PEED
				WHERE MEMBER_SEQ=#{seq}
	</select>
	
	<select id="countFollow" parameterType="sist.co.model.MemberDTO" resultType="int">
		SELECT /* +INDEX_FFS(INSTA_MEMBER,SYS_C007779) */ NVL(COUNT(*),0) 
			FROM INSTA_MEMBER
				WHERE SEQ IN(SELECT INSTA_FOLLOW.FOLLOW
								FROM INSTA_MEMBER, INSTA_FOLLOW
									WHERE INSTA_FOLLOW.MEMBER_SEQ=#{seq})
		
	</select>
	
	<select id="countFollower" parameterType="sist.co.model.MemberDTO" resultType="int">
		SELECT /* +INDEX_FFS(INSTA_MEMBER,SYS_C007779) */ NVL(COUNT(*),0) 
			FROM INSTA_MEMBER
				WHERE SEQ IN(SELECT MEMBER_SEQ
								FROM INSTA_FOLLOW
									WHERE FOLLOW=#{seq})
		
	</select>
	
	<select id="findMemberDTO" parameterType="int" resultType="sist.co.model.MemberDTO">
		SELECT *
			FROM INSTA_MEMBER
				WHERE SEQ=#{seq}
	</select>
	
	<select id="getPeedList" parameterType="int" resultType="sist.co.model.PeedDTO">
		SELECT *
			FROM INSTA_PEED
				WHERE MEMBER_SEQ=#{seq} 
	</select>
	

</mapper>  